data:
  data_dir: "./data"
  num_workers: 2
  batch_size: 128
  valid_split: 0.1

train:
  epochs: 30
  learning_rate: 0.001
  momentum: 0.9
  weight_decay: 5e-4
  lr_step_size: 10
  seed: 42

model:
  name: "ResNet18"

output:
  run_dir: "./runs/exp2"
  save_model: "best_model.pth"